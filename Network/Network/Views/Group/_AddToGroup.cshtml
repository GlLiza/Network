@model Network.DAL.EFModel.MembersOfGroup


<div id="addToGroupDialog">
    <div class="modal-dialog ">
        <div class="modal-content">

            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal">&times;</button>
                <h4 class="modal-title">Добавление в групу=пу</h4>
            </div>

            <div class="modal-body">
                @using (Html.BeginForm("AddToGroup", "Group", FormMethod.Post))
                {
                    @Html.HiddenFor(model => model.GroupId)

                    @*<div class="form-group">
                        @Html.DropDownListFor(model => model.selectedGroupId, new SelectList(Model.groupList, "Id", "selectedGroupId"), "Выбери группу")
                    </div>

                    <div class="form-group">
                        @Html.DropDownListFor(model => model.selectedSpec, new SelectList(Model.groupList, "Id", "selectedSpec"), "Выбери специализацию")
                    </div>*@

                    <button class="btn btn-info pull-right" type="submit">OK</button>
                }
            </div>
        </div>
    </div>
</div>

